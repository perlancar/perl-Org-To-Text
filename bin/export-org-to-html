#!/usr/bin/perl

use 5.010;
use strict;
use warnings;

use Sub::Spec::CmdLine qw(run);
run(module => 'Org::Export::HTML', sub=>'export_org_to_html');

# ABSTRACT: Export Org to HTML document
# PODNAME: export-org-to-html
